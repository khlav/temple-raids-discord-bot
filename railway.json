{
  "build": {
    "builder": "RAILPACK",
    "buildCommand": "sh -c 'export HUSKY=0 && pnpm install --ignore-scripts && npx tsc -p tsconfig.prod.json && pnpm prune --prod'"
  },
  "deploy": {
    "startCommand": "node dist/index.js",
    "restartPolicyType": "ON_FAILURE",
    "restartPolicyMaxRetries": 10
  }
}
