{
  "build": {
    "builder": "RAILPACK",
    "buildCommand": "pnpm install --ignore-scripts && pnpm build && pnpm prune --prod"

  },
  "deploy": {
    "startCommand": "node dist/index.js",
    "restartPolicyType": "ON_FAILURE",
    "restartPolicyMaxRetries": 10
  }
}
